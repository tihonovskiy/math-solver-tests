package pages.components;

import pages.BasePage;

/**
 * Base class for reusable UI components (e.g. TabBar, search bars, navigation headers).
 */
public abstract class BaseComponent extends BasePage {
}
